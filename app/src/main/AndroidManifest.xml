<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.resatravels">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_admin_for_facility_delete"></activity>
        <activity android:name=".user_login" />
        <activity android:name=".Rules" />
        <activity android:name=".activity_admin_for_availability_Update" />
        <activity android:name=".admin_for_availability_insert" />
        <activity android:name=".admin_for_insert_hotel" />
        <activity android:name=".Show_Hotels_Admin" />
        <activity android:name=".Admin_For_Update" />
        <activity android:name=".admin_for_delete_hotel" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>